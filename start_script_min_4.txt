# Проверка содержимого корня
ls

# Переход в папку docs
cd docs

# Просмотр содержимого папки docs
ls

# Чтение текстового файла
cat readme.txt

# Проверка работы uniq (уникальные строки)
uniq duplicates.txt

# Проверка работы uniq с подсчётом повторов
uniq -c duplicates.txt

# Возврат в корень
cd ..

# Проверка echo
echo Привет из эмулятора
echo Hello World 123

# Чтение файла в корне
cat file.txt

# Проверка бинарного файла
cat binfile.bin

# --- Блок с намеренными ошибками ---
# Попытка прочитать несуществующий файл
cat not_exist.txt

# Попытка прочитать директорию
cat docs

# Попытка перейти в несуществующую директорию
cd not_exist

# Попытка перейти в файл
cd file.txt

# Попытка uniq на несуществующем файле
uniq not_exist.txt

# Попытка uniq на директории
uniq docs

# --- Завершение работы ---

